name: trash_component
description: A well written collection of Flutter component that will solve some problem when the user need to copy and paste dart code
version: 0.0.3
homepage: https://github.com/vincenzopalazzo/trash

environment:
  sdk: ">=2.15.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
